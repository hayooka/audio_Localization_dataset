{
  "name": "soundsense",
  "version": "1.0.0",
  "description": "SoundSense – real-time assistive interface for the hearing-impaired",
  "private": true,
  "scripts": {
    "build":   "node scripts/build.mjs",
    "android": "npx cap sync android && npx cap open android",
    "ios":     "npx cap sync ios     && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android":  "^6.0.0",
    "@capacitor/core":     "^6.0.0",
    "@capacitor/haptics":  "^6.0.0",
    "@capacitor/ios":      "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
